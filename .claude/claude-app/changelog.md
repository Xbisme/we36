# We36 — Changelog

> Append-only ship history. One entry per spec as it lands. Newest at top.
> Format: `### YYYY-MM-DD — Spec #NNN <Name> ✅ <verb>` + 1–4 bullets.
> (See [`dev-workflow.md`](dev-workflow.md) §Changelog Entry Format for the `<verb>` vocabulary.)

---

### 2026-07-11 — Spec #014 Settings, Privacy & Safety 🔵 IMPLEMENTED (72/72; pending PR/merge)

- **Built (US1–US6 + most of US7, 67/72 tasks; full suite 723 green)**: the **Settings** surface (Screens 30–32) + the privacy/safety controls other features enforce, as a **contract-driven client** over the already-shipped backend (B#014 source-verified against `backend/src/modules/{users,social,moderation,stories}`). **US1** — grouped Settings hub off the profile gear (Account/Privacy/Notifications/Language/Theme/About), **About** version/build via `package_info_plus`, logout→confirm→`SessionController.signOut`. **US2** — **private-account** toggle (optimistic PATCH `/me/settings`) + the new owner-side **follow-request approval inbox** (`GET /me/follow-requests`, Approve→`RelationshipStore` update, Decline; optimistic + rollback + idempotent). **US3** — **blocking** (a new core `BlockedUsersStore` seam so feed + DM hide blocked authors reactively in-session via `filterBlocked`; mutual-sever through `BlockActions`; **Blocked accounts** management screen) + **report** (fixed 9-reason backend-aligned set, surface-only ack) reachable from **profile, reel, comment, DM chat, and story viewer** (Constitution I — closed the analyze C1 gap). **US4** — **close-friends** list management (view/add via candidate picker/remove; feeds the #005 stories audience). **US5** — device-scoped **language** (EN/VI/System) + **theme** (Light/Dark/System) via a new `AppPreferences` (shared_preferences) + app-level `AppSettingsCubit` feeding `MaterialApp.router` (`We36App` now consumes it); persists across launches **and logout** (FR-027/033). **US6** — **activity status** toggle with **reciprocal** presence hiding via a core `PresenceVisibility` seam gated in `MessagingRealtimeService` (closed analyze A1) + entry-only **Two-factor** and **Download-your-data** screens. Runs DI `environment: 'fake'` (zero-network); **no drift schema change** (stays v10 — session in-memory stores cleared on logout).
- **Tech notes**: new `lib/core/data/{settings,social,moderation,close_friends}/` slices (each real `env:['real']` + fake `env:['fake']` repo, source-reconciled DTOs), core seams `BlockedUsersStore`/`BlockActions`/`PresenceVisibility`/`AppPreferences`, shared `SettingsRow`/`SettingsSectionHeader`/`showReportSheet`/`showBlockReportActions` in `core/presentation/`, `lib/features/settings/` (6 cubits — settings/follow-requests/blocked-accounts/close-friends/app-settings + pages), `BlockedUsersStore.clear()` wired into `SessionController._clearSession()`. **Reused**: `RelationshipStore` (#010), messaging presence (#012), `StoryAudience.closeFriends` (#005), `CursorPage`/`PaginatedListCubit` (#002). **New dep**: `package_info_plus` (About only; no `in_app_review`). **FR-019 reconciled** to the backend `ReportReason` enum (dropped "scam", added "intellectual-property"). **Bug fixed**: bootstrap must NOT block on the prefs read (hung the launch smoke test) — `AppSettingsCubit.load()` is non-blocking + defensive. EN+VI ARB extended. `flutter analyze` clean (2 pre-existing pubspec infos); **723 tests green**.
- **Completed the final 5 (now 72/72)**: T037 block filter extended to **explore** + **notifications** (`filterBlocked` in `WatchExplore`/`WatchNotifications`; search relies on server-side blocked-exclusion #009 + fresh re-query); T041 **comment report** rewired to the unified `showReportSheet` (reason picker); T008 `SettingsRow` golden; T044 `BlockedAccountsPage` golden + `showReportSheet`/`showBlockReportActions` widget test (T071 — DM + story reachability); the report sheet was also made scrollable (a11y). **738 tests green.**
- **Backend — `GET /me/blocks` implemented (closes the last B#014 gap)**: the blocked-accounts list endpoint was added to the social module (`social.controller`/`service`/`repository` + `dto/blocked-user.dto.ts`) — a cursor page `{items: UserSummaryDto[], nextCursor, hasMore}` newest-first keyset over the `Block` table, avatars resolved via `MediaService.resolveAvatarUrls`, mirroring the followers/following list. Client `RealBlockRepository.listBlocked()` binds directly (TODO removed). `tsc --noEmit` + `eslint` clean. Verified statically (mirrors the proven list pattern); needs a backend (re)start to exercise live.
- **Cutover finished**: close-friends `candidates()` now binds to the current user's followers (`GET /users/:myId/followers` via `MeRepository` for the id → `UserSummary` items — the accounts eligible to be added, since the backend requires a candidate to already follow you); `S3_ENDPOINT` set to the LAN IP so on-device presigned uploads (avatar/create-post) work. **No pending B#014 gaps remain.**
- **Clarifications locked** (from `/speckit.clarify`): private-account **non-retroactive** · block **mutual-severing** (unblock doesn't restore follow) · activity status **reciprocal** · report **fixed set, no free-text** · language/theme **device-scoped**.
- **Dev media-on-device fix (client-side, landed on this branch)**: on a **physical** iPhone all media (feed images, reel video, avatars) was broken because the backend derives media URLs from its `localhost:9000` object store — `localhost` = the phone on device (Connection refused), while the Simulator resolves it to the dev Mac. Fixed entirely client-side, dev-gated, in **three layers** (all no-op in prod / when the API host is localhost): (1) **`DevMediaHttpOverrides`** — an `HttpOverrides` installed at bootstrap that rewrites `localhost`/`127.0.0.1` → the LAN API host at the network layer, so **every Dart image load** (`NetworkImage`, `CachedNetworkImage`, avatars, thumbnails — fresh OR drift-cached) is fixed in one place with zero per-widget edits; (2) **`DevMediaUrlInterceptor`** on the `ApiClient` deep-rewrites response URLs before decode, keeping the drift cache itself clean; (3) the reel **`VideoReelPlayer`** rewrites its own rendition URL since native `video_player` (AVPlayer) bypasses Dart's `HttpClient`. Shared pure helper `rewriteLocalhostUrl(url, host)` + a `DevMediaHost` host set at bootstrap. Unit-tested (nested rewrite, port/path preserved, non-URL strings + prod URLs untouched); `HttpClient` forwarding delegates all members to the inner client.
- **Backend fix — feed/reel/DM author avatars (dogfood)**: post/reel authors + DM participants/senders showed a gray placeholder because their backend summaries returned only `avatarMediaId` and left `avatarUrl` **null** (only the #010 profile/social surfaces resolved it — the DTO comment literally said "null on surfaces not yet wired"). Wired all three: `posts.service` `projectPosts`/`projectReels` batch-resolve author + tagged-user avatar URLs via `MediaService.resolveAvatarUrls` (new `resolveAvatars()` helper) threaded into `toSummary`; `messaging.service.toUserSummary` is now async and resolves the participant/sender avatar inline (both callers awaited). Client already parses `avatarUrl`; combined with the dev HttpOverrides (rewrites the resolved `localhost:9000` avatar URL → LAN on device) avatars now load. `tsc --noEmit` clean. Needs a backend restart + a feed/DM refresh.
- **Realtime hardening (dogfood fix, landed on this branch)**: real-mode boot surfaced an **uncaught** `WebSocketConnectionClosed` from `socket_io_client`'s own destroy microtask (a socket_io_client × `web_socket` rough edge on an already-closed socket) — it escaped to `platformDispatcher.onError`. Now `SocketIoRealtimeClient` owns the whole socket lifecycle (connect **and** teardown) in one **guarded `Zone`**; a benign transport-close is classified (`isBenignRealtimeDisconnect`) and **degraded quietly** to a `warn` + `RtReconnecting` (Constitution VIII), anything else logged without crashing. Unit-tested classifier; needs an on-device real-mode re-run to confirm the uncaught error is gone.

### 2026-07-08 — Spec #013 Notifications & Push ✅ MERGED INTO MAIN (PR #13) (56/56)

- **Built (US1–US6, 56/56 tasks)**: the **Activity** surface (Screen 29) + real **push** delivery, and the **second live wiring** of the #002 realtime socket (`notification.new`, after #012 DM). **US1** — the grouped, **time-sectioned** feed (New / This week / Earlier), server-grouped rows ("Alice and N others") with actor avatars (name-initial fallback — server omits avatars), per-type summary, target thumbnail, relative time, **unread accent**; **deleted-target rows degrade** non-tappably (FR-006); **cursor pagination** + **pull-to-refresh**; **mark-all-read on open** clears the badge (FR-008); tap **deep-links** (post/reel/comment → post detail; follow/mention-user → profile). **US2** — contextual push **permission affordance** (first Activity open, no auto-renag), device **register/unregister** over REST (`PushRegistrationService`, token = credential, never logged), `SessionController` unregisters on logout, **coarse deep-link** (`pushTapRoute`: DM→Messages, feed→Activity). **US3** — unread **badge** on the Home-header bell (phone) + SidebarRail item (tablet) via the `NotificationsBadge` core→core seam. **US4** — live `notification.new` folds into the one canonical cache silently (no banner, Q4), dedupe-by-id (exactly once, SC-004), offline-from-cache. **US5** — inline **follow-back** (optimistic + rollback, reuses #010 toggle); follow-request rows are route-only (approval → #014, Q1). **US6** — a11y (SR labels, 2× text, light/dark), phone↔tablet reflow, log-redaction. drift schema **v9→v10** (additive `Notifications` table, wiped on logout). Runs DI `environment: 'fake'` (zero-network, **no Firebase**) for hermetic tests.
- **Tech notes**: new `lib/core/data/notifications/` (`NotificationEntry`+`NotificationType`/`ActorCard`/`NotificationTarget`/`TargetKind` freezed, DTO mappers, `NotificationsRepository` real [`env:['real']`] + `FakeNotificationsRepository` [`env:['fake']`], remote source) + `lib/core/data/cache/` (`Notifications` table + DAO) + `lib/core/services/{realtime/NotificationsRealtimeService, notifications/NotificationsBadge, push/(PushService·FirebasePushService·FakePushService·PushRegistrationService)}` + `lib/features/notifications/` (`NotificationsCubit`/`PushPermissionCubit` 4-state, usecases, `NotificationsPage`, widgets [`notification_tile`/`notification_section_header`/`follow_back_button`/`push_permission_prompt`]). **Corrected the #002 `notification.new` scaffold parser** — it read a non-existent `notification` wrapper; the source-verified backend emits `{entry, unreadCount}` (regression-locked). **B#013 is source-verified** against `backend/src/modules/notifications` (no post-hoc reconcile expected). **New deps** `firebase_core ^4.11.0` · `firebase_messaging ^16.4.1` · `flutter_local_notifications ^22.0.1`. **Native gates cleared**: iOS deployment target **13→15** (FlutterFire drops 13/14) + `remote-notification` background mode; Android already compliant (Java 17 / AGP 8.11.1 / compileSdk 35). Excluded `build/**` from analysis (the Firebase iOS SPM checkout ships its own Dart test files). EN+VI ARB extended. **`flutter analyze` clean** (2 pre-existing pubspec-sort infos); full #013 suite green (dto/dao/migration v9→v10/cubit/page/goldens/a11y/redaction/realtime-fold/badge/push-gate/follow-back).
- **Clarifications locked**: follow-request approval **deferred to #014** · **real push wiring now behind a seam**, credentials deferred to cutover/#015 · permission prompt on **first Activity open**, no renag · foreground live event = **silent** badge+feed (no banner) · feed sections **New / This week / Earlier**.
- **Firebase config WIRED (dev flavor)**: the real `we36-ed5a4` Firebase project config is now in the repo. **Android** — `android/app/src/dev/google-services.json` (matches `app.we36.dev`) + the `com.google.gms.google-services` Gradle plugin applied (`settings.gradle.kts` 4.4.2 + `app/build.gradle.kts`). **iOS** — `ios/Runner/GoogleService-Info.plist` placed **and added to the Xcode Runner target** (4 `project.pbxproj` entries: file ref in the Runner group + build file in the Resources phase; `plutil -lint` OK, `xcodebuild -list` parses) so it bundles automatically. App-root **push-tap deep-linking wired** in `bootstrap.dart` (`onNotificationTap` + cold-start `getInitialMessage` → `pushTapRoute`; no-op in fake mode). `FirebasePushService` lazy-inits `Firebase.initializeApp()` guarded on config presence.
- **FCM background handler WIRED**: top-level `firebaseMessagingBackgroundHandler` (`@pragma('vm:entry-point')`, own file, self-inits Firebase) registered via `FirebaseMessaging.onBackgroundMessage` in `FirebasePushService.initialize()`; `bootstrap` calls `PushService.initialize()` at startup (real: Firebase up + background handler; no-op in fake). So a killed/backgrounded app receives pushes (the OS auto-displays the `notification` payload).
- **Remaining for full on-device push**: (1) **APNs auth key** uploaded to the Firebase console (iOS — required, Apple Developer Program needed); (2) **prod** `google-services.json` / plist for `app.we36`(.prod) at prod cutover; (3) run the **real DI env** against a live backend (`--flavor dev --dart-define=DI_ENV=real`) to exercise `POST /devices`; (4) on-device receipt smoke (foreground/background/killed). The **Android** Gradle wiring couldn't be compile-verified in this environment (no Android SDK) — verify with `flutter build apk --flavor dev`; **iOS** project integrity verified (`plutil`/`xcodebuild -list`). `bloc_tools:bloc lint` still no local CLI.

### 2026-07-08 — Spec #012 Direct Messages (Realtime) ✅ MERGED INTO MAIN (PR #12) (59/59)

- **Shipped (US1–US6, 59/59 tasks; 625 tests green)**: the **private 1-1 messaging** surface (Screens 25–28) + the **first live wiring of the #002 realtime channel** — conversation list (unread, coarse presence, typing preview) + 1-1 **chat** over the Socket.IO channel (text, **photo**, **shared post**, **stickers**) with **optimistic send + idempotency + delivery state** (`sending→sent→read`, collapses gracefully) + typing/presence + **new-message compose** + tablet **master/detail two-pane** (select swaps the pane, no push; phone keeps push). Architecture = **REST-send + inbound-authoritative socket** (idempotent via the Idempotency-Key header; one canonical cached copy per conversation/message; outbox flush on reconnect). `RealtimeConnectionManager` (connect-on-auth / disconnect-on-logout) + `MessagingRealtimeService` (folds inbound → cache + transient typing/presence). Cross-feature entry via `MessagingLauncher` seam (profile "Message" + post "share to DM" — no cross-feature import, Constitution XI). drift schema **v8→v9** (`Conversations` + `Messages`/outbox tables; wiped on logout). **No new pub dependency.** Runs DI `environment: 'fake'` (`FakeRealtimeClient` scripts realtime) for hermetic tests.
- **Backend reconcile (`7d046a3`, source-verified)**: the B#012 shapes were DERIVED at plan time, then reconciled against the **actual backend source** (`backend/src/modules/messaging`) rather than guessed — fixing several real mismatches: conversation list is `GET /conversations` (not `/me/conversations`, fixed a 404); start body `{userId}`; send body flat `{kind, body|mediaId|sharedPostId|stickerId}` with idempotency via **header** (no `clientKey` field); a photo maps to wire `kind='media'`; `ConversationDto.otherUser`; `isMine` computed from `senderId == current user`; list surfaces **both** `filter=primary` + `filter=requests` in one list (no separate requests-inbox in v1.0). Realtime: `message.new` payload **is** the flat `MessageDto` emitted to `[self, other]` → the sender gets an **echo of their own send** (dedup by `serverId` + skip echoes where `senderId == me` via `MeProfile` cache); **no `message.delivered` event** (sent→read only); `message.read = {conversationId, userId, upToMessageId}`; `typing.isTyping` honored. DAO upsert **dedups/collapses by `serverId`** (self-heals devices that accumulated duplicates pre-fix). Contract docs marked verified.
- **Tech notes**: new `lib/core/data/messaging/` (`Message`+`MessageKind`/`DeliveryState`/`MessageContent` union/`PostRef`, `Conversation` freezed; `MessagingRepository` real [`env:['real']`] + `FakeMessagingRepository` [`env:['fake']`], remote source, wire DTO mappers) + `lib/core/data/cache/` (`Conversations` + `Messages`/outbox tables + DAO) + `lib/core/services/realtime/` (`RealtimeConnectionManager` + `MessagingRealtimeService`) + `lib/core/services/messaging/MessagingBadge` (core→core unread seam) + `lib/features/messaging/` (`conversations`/`chat`/`new_message`/`messaging_shell` 4-state cubits, usecases, pages, widgets [`conversation_tile`/`active_now_rail`/`message_bubble`/`delivery_status`/`typing_indicator`/`chat_composer`/`shared_post_card`/`connection_banner`/`chat_photo_picker`]). `SessionController` now brings the socket up/down. Reuses `RealtimeClient`/`SocketEvents` (#002), `UserSummary` (#002/#010), #007 media pipeline (photo), sticker tray + `TwoPaneScaffold` (#001), `CursorPage`/`PaginatedListCubit` (#002). EN+VI ARB extended.
- **Clarifications locked**: any non-blocked user messageable · **no requests-inbox** (→#014) · delivery 4-state w/ graceful collapse · **coarse presence** (Active now + typing, no last-seen) · photo = quick pick→upload→send (no editor).
- **Follow-ups**: interactive on-device DM walkthrough → #015 release gate (automated fake-mode coverage authoritative); `RealMessagingRepository` already source-reconciled to B#012 — final smoke at dev-backend cutover; **message push** lands with **#013**; group chats / calls / message-requests inbox deferred; `bloc_tools:bloc lint` still no local CLI.

### 2026-07-07 — Spec #011 Saved Collections ✅ MERGED INTO MAIN (PR #11) (51/51)

- **Shipped (US1–US5 + Polish, 51/51 tasks)**: the **save-and-organize** surface — the profile **Saved** tab (Screen 24, owner-only) opening a 2-column grid of collection cards (4-image quilt cover + name + "N saved") with a synthetic **"All saved" always first** + a create (`+`) action (US1); **save a post into a collection** (US2) — the default bookmark saves silently to All saved, an explicit **"Save to collection"** sheet files into an existing/new collection, **optimistic + idempotent + rollback** (retried file ⇒ exactly one membership, SC-004; filing an unsaved post also sets the canonical `viewerHasSaved` flag); **open & curate** (US3) — a cursor-paginated item grid (reuses #009 `DiscoveryGrid`/`DiscoveryGridTile`, reels marked, tile → #006 post detail), remove-from-collection (keeps All saved, SC-005), **full unsave** confirm-gated on `namedMembershipCount` (removes everywhere + cascades the canonical flag, SC-007); **manage a collection** (US4) — rename / delete (confirm, keeps posts saved, SC-006) / set-cover via an action sheet, **hidden for the default "All saved"**, cover **auto-fallback** when the cover item is removed/unsaved (FR-010); **US5** inclusive/adaptive hardening (screen-reader labels on cards/tiles/actions, 2× text scale, light/dark, phone 2-col ↔ tablet centered-mobile fallback). drift schema **v7→v8** (additive `SavedCollections` table + DAO, cleared on logout via `SessionController`). Runs DI `environment: 'fake'` (zero-network) for hermetic tests.
- **Tech notes**: new `lib/core/data/collections/` slice (`SavedCollection` + `PostCollectionsMembership`/`PickerRow` freezed, `CollectionsRepository` real [`env:['real']`] + `FakeCollectionsRepository` [`env:['fake']`], remote source, `saved_collections_table` + `saved_collections_dao` as the one canonical collections cache) + `lib/features/collections/` (`collections`/`save_to_collection`/`collection_detail`/`collection_edit` 4-state cubits, usecases [collections/save-to-collection/collection-items/manage], `saved_collections_view`/`collection_detail_page`, widgets [`collection_card`/`collections_grid`/`save_to_collection_sheet`/`create_collection_dialog`/`collection_more_sheet`]). **Cross-feature render seam** (Constitution XI): `SavedTabSlot` in `core/presentation/slots/` (impl `SavedTabSlotImpl` in `features/collections/`) so `features/profile` renders the Saved tab via `getIt<SavedTabSlot>()` — **no direct `features/collections` import**; the save-to-collection entry point wires in via a similar launcher seam. Reuses shipped `Post`/`viewerHasSaved` + `FeedRepository.save` (#004), `ExploreItem`/`DiscoveryGrid` (#009), `CursorPage`/`PaginatedListCubit` (#002), `ProfileTabBar` (#010). **No new pub dependency.** EN+VI ARB extended. Full collections test suite (dao/fake-repo/4 cubits/widget/goldens/a11y/redaction).
- **Backend reconcile (`97ec1d8`)**: `contracts/collections-api.md` reconciled with the shipped **B#011** — actual endpoints live under `/collections` (not `/me`), add-item body is `{postId}` → `204`, cover is a single **derived** `MediaDto`. Three backend deviations from the spec recorded for the real cutover: (1) collection **names are unique** (spec assumed non-unique), (2) **no set-cover endpoint** (cover auto-derived server-side), (3) **no per-post membership endpoint** (Save-to-collection files additively). App still on fake mode, so these bind at `RealCollectionsRepository` cutover.
- **Follow-ups**: `RealCollectionsRepository` cutover at dev-backend integration (honor the 3 B#011 deviations above); interactive on-device Saved-tab walkthrough → #015 release gate (automated fake-mode coverage authoritative); `bloc_tools:bloc lint` still no local CLI.

### 2026-07-07 — Spec #010 Profile & Follow ✅ MERGED INTO MAIN (PR #10) (57/57)

- **Shipped (US1–US6, 57/57 tasks)**: the **identity + social-graph** surface — **My profile** (Screen 20, the fifth tab's landing) with avatar/name/username/bio/website + posts/followers/following counts and a **Posts + Tagged** cursor-paginated grid (reels marked), entry points to Edit profile / Settings / Create / Share; **Other-user profile** (Screen 21) with **optimistic follow** (instant flip + follower-count bump, rollback on failure, idempotent retry → exactly one follow), **confirm-on-unfollow** (and withdraw-request) dialog, "Follows you" indication, surface-only Message/report/block; **Followers & following lists** (Screen 22) — two tabs with in-list search, per-row relationship control (same optimistic toggle), pagination, blocked accounts excluded; **viewer-side private accounts** (US4) — Follow → **Requested**, header + counts render but the content grid **and** followers/following lists are gated behind a "This account is private" notice; **Edit profile** (Screen 23, US5) incl. **change avatar** (pick → crop → upload via the #007 media pipeline, writes `avatarMediaId` — completes the avatar #003 deferred); **US6** inclusive/adaptive hardening (screen-reader labels, large-text, light/dark, phone↔tablet reflow). Own **following count** updates canonically + optimistically on the viewer's own profile (SC-004). Contract-driven on the shipped backend **B#010**; the client enforces no visibility rule the API hasn't. Runs DI `environment: 'fake'` (zero-network) for hermetic tests.
- **Tech notes**: new `lib/core/data/profile/` slice (`ProfileView` + `AccountRow` freezed, `ProfileRepository` real [`env:['real']`] + `FakeProfileRepository` [`env:['fake']`], `profile_remote_data_source`, in-memory `relationship_store` as the one canonical relationship copy) + `lib/features/profile/` (`my_profile`/`profile`/`follow_list`/`edit_profile` 4-state cubits, usecases [profile/follow/follow-list/edit], `my_profile_page`/`user_profile_page`/`follow_list_page`/`edit_profile_page`, widgets [`profile_header`/`profile_stats`/`profile_tab_bar`/`profile_grid`/`follow_button`/`account_row_tile`/`private_gate`/`avatar_picker_sheet`]). Reuses shipped models `MeProfile` (#003) / `User` (#002) / `ViewerRelationship` (#009) / feed `Post`+grid (#004). **No drift schema change** (relationship state lives in the in-memory store; drift stays **v7**), **no new pub dependency**. EN+VI ARB extended (+170 lines each). `flutter analyze` = 2 pre-existing pubspec-sort infos; `dart format` clean.
- **Post-merge gate fix (2026-07-07, on `main`) — 4 unrelated failures cleaned up → 509 pass / 0 fail**: PR #10 merged with the gate RED on 4 pre-existing failures unrelated to #010's code (stories & post-detail logic untouched). (1) **2× #005 stories** (`stories_repository_impl_test`: "prepends the own Your story reel" / "a tray fetch failure still surfaces the own reel") were a **time-bomb** — the test seeded a fixed `createdAt: 2026-07-02` own segment but built `OwnStoryStore()` with the **real** wall-clock (24h TTL, `own_story_store.dart:8`), so once real time passed seed + 24h the own story was filtered as expired. **Fixed** by freezing the store's injectable clock in `setUp` (`OwnStoryStore(clock: () => DateTime.utc(2026, 7, 2, 12))`) — deterministic, no production change. (2) **2× #006 goldens** (`post_detail_goldens_test` comment list light/dark) = the recurring **0.06%/297px sub-pixel AA drift** — regenerated. Full gate now green: **509 tests pass**, `flutter analyze` = 2 pre-existing pubspec-sort infos, `dart format` clean.
- **Clarifications locked**: B#010 shipped (contract-driven) · avatar change **included** (not deferred) · grid tabs = **Posts + Tagged** (Saved → #011) · private scope = **viewer-side only** (approving requests → #013/#014) · **confirm on unfollow/withdraw only** (Follow stays instant) · private account = **counts shown, lists gated** · viewer's own following count updates canonically + optimistically.
- **Follow-ups**: interactive on-device profile/follow walkthrough → #015 release gate (automated fake-mode coverage authoritative); `RealProfileRepository` cutover at dev-backend integration; the profile **Saved** tab + collections land with **#011**; approving incoming follow-requests deferred to **#013/#014**; `bloc_tools:bloc lint` still no local CLI.

### 2026-07-03 — Spec #009 Explore & Search ✅ MERGED INTO MAIN (PR #9) + gate closed (54/54)

- **Shipped (US1–US5, 54/54 tasks)**: the **discovery** surface — **Search** (Screens 17–18) live as-you-type (debounced ~300 ms, ≥2 chars) across **Top / Accounts / Tags / Places** tabs (Top = fixed blended snapshot with "see more"; single-type tabs cursor-paginate), read-only relationship state on account rows, blocked-user exclusion + private-account-findable-by-handle, latest-term-wins; **Explore grid** (Screen 16) — a quilted 3-col phone layout (2×2 hero) / responsive tablet grid of mixed post+reel tiles (reels marked), **persisted on-device** (drift, offline cold-start → background reconcile), pull-to-refresh + infinite scroll, category shortcut chips; **Recents** (US3) newest-first, promote-not-duplicate, per-row delete + clear-all; **Hashtag/place pages** (Screen 19) header + cursor grid, surface-only Follow (toast); **US5** a11y/adaptive hardening (screen-reader tile/row labels, large-text, light/dark, phone↔tablet reflow). Non-personalized (no ranking); backend B#009 enforces all visibility/block rules. drift **v5→v7** (`ExploreItems` persisted). Runs DI `environment: 'fake'` (zero-network).
- **Tech notes**: new `lib/core/data/discovery/` slice (`ExploreItem` [kind-tagged #004 `Post` / #008 `Reel` projection], `SearchResults`/`SearchRecent`, `DiscoveryRepository` real + `FakeDiscoveryRepository`, remote source, `explore_items_table` + `explore_dao`) + `lib/features/explore/` (`ExploreCubit`/`SearchCubit`/`RecentsCubit`/`DiscoveryGridCubit` 4-state, discovery/search/recents/explore use cases, `explore_page`/`search_page`/`discovery_grid_page`, widgets [`discovery_grid`/`discovery_grid_tile`/`category_chips`/`results_tabs`/`result_rows`/`account_result_row`/`recents_section`]). No new pub dependency.
- **Gate closed this session (T054)**: PR #9 merged with a **red gate** (the one open task) — 3 explore widget tests **deadlocked** (drove the real `ExploreCubit` over drift `NativeDatabase` inside `testWidgets`, so `loadInitial`'s `await _watch().first` never resolves in the faked-async zone → 0% CPU hang until the 10-min timeout; this is what made `flutter test` appear to run ~18 min) + 2 **#006 post-detail goldens** drifted 0.08% (AA sub-pixel, `comment_list` light/dark). **Fix is test-only — no production-code bug** (the grid's `NotificationListener` paginates only on genuine scroll): added `StubExploreCubit` (mirrors `StubFeedCubit`) so `explore_page_test` + `app_shell_test` render a seeded `ExploreState.loaded` synchronously; regenerated the 2 goldens. **446 tests pass**; `flutter analyze` = 2 pre-existing pubspec-sort infos; `dart format` clean.
- **Follow-ups**: on-device long-scroll memory profiling of the discovery grids → #015 (automated ≥5-page pagination coverage authoritative); account-row Follow + `@mention`/avatar nav land with **#010 Profile & Follow**; hashtag-follow is surface-only (deferred); real backend discovery cutover at dev-backend integration; `bloc_tools:bloc lint` still no local CLI.

### 2026-07-03 — Spec #008 Reels ✅ MERGED INTO MAIN (PR #8) + polish/gate closed (57/57)

- **Shipped (US1–US4 + Polish, 57/57 tasks)**: the **Reels** surface (Screen 10) — a full-screen vertical `PageView` with a **disciplined video lifecycle** (`ReelPlaybackController`: only the visible reel plays looping; off-screen players pause + dispose; bounded ±1 preload window — Constitution II), an **action rail** (optimistic like/save via the cubit, comment bottom-sheet, surface-only share/follow, overflow report/delete-own), reel **comments** (reuses the #006 comment slice seam), **create reel** (pick video → caption → publish, sharing the #007 upload pipeline; optimistic just-published reel carries a `ProcessingBadge` until the ready rendition reconciles), and empty/loading/error-retry states from the 4-state cubit. drift **v4→v5** (Reels table). Runs DI `environment: 'fake'` (zero-network).
- **Polish/gate closed this session (the 5 tasks PR #8 merged open — T047/T048/T051/T052/T055)**: **T047** iOS audio session honors the hardware silent switch via a minimal `ReelAudioSession` method-channel (`we36/reel_audio` → `AVAudioSession.setCategory(.ambient)` in `AppDelegate.swift`) — **no new package** (research R3 channel-first); Android plays per stream volume; no-op off iOS. **T048** adaptive layout (FR-028): `LayoutBuilder` keeps phones (<700) full-bleed but centers a portrait 9:16 column at ≥700 instead of stretching. **T051** reels log-redaction test (no `print`/`debugPrint`; no logger leaks `videoUrl`/`posterUrl`/tokens/byte refs). **T052** goldens for `ReelActionRail` + `ProcessingBadge` (light+dark) — `_ProcessingBadge` extracted to a public `widgets/processing_badge.dart` per T044's intent. **T055** pre-commit gate: `dart format` clean, `flutter analyze` = 2 pre-existing pubspec-sort infos (fixed an in-scope `curly_braces` lint), **396 tests pass**.
- **Tech notes**: new `lib/features/reels/` (domain `reel_draft` + usecases [feed/engagement/comment/publish], `reels`/`reel_compose`/`reel_comments` cubits, `ReelPlaybackController` + `ReelPlayer` seam over `video_player`, `reels_page`/`reel_compose_page`, widgets [`reel_view`/`reel_action_rail`/`processing_badge`/`reel_comments_sheet`/`reel_more_sheet`]) + `lib/core/data/reels/` (`Reel` freezed [a `Post kind=reel` projection reusing feed `UserSummary`/`Media`], repository real + `FakeReelsRepository`, remote source). New deps `video_player`/`visibility_detector` (verified at plan). Also regenerated 2 stale **#006 post-detail goldens** that were sub-pixel-drifting on `main` pre-session (unrelated to reels; format-only diffs proved layout unchanged).
- **Follow-ups**: on-device silent-switch + quickstart US1–US4 walkthrough → #015 release gate (automated fake-mode coverage authoritative + green); `RealReelsRepository` cutover at dev-backend integration (B#007 `kind=reel`); `bloc_tools:bloc lint` still no local CLI.

### 2026-07-02 — Spec #006 Post Detail & Comments ✅ MERGED INTO MAIN (PR #7) (46/46)

- **Built (US1–US6 + Polish, 46/46 tasks)**: full-screen nav-less **Post detail** (Screen 14) off the feed `PostCard` (tap the comment action → `/post/:id`) rendering the canonical #004 `PostCard` + a **cursor-paginated, oldest-first Comments** surface (Screen 15) — **one-level replies** interleaved & indented, a **quick-emoji** row (inserts into the input), **optimistic + idempotent** add/reply (client `clientKey` = Idempotency-Key; retry ⇒ exactly one), optimistic **comment like** (target-state, revert, last-intent), **delete-own** (action-sheet + confirm, cascade, count −(1+replies)) / **report-other** (surface-only ack), `@mention`/`#hashtag` styled violet (non-tappable), and empty/loading/error-retry states. **commentsDisabled** hides the input. On **tablet/iPad** the detail is a **single-post two-column split** (media pane on `#0E0E1A` | comments+input), built with `AppBreakpoints` (not `TwoPaneScaffold`); phone keeps push. Runs DI `environment:'fake'` (zero-network).
- **Tech notes**: new `lib/core/data/comments/` slice (`Comment`/`CommentAuthor`/`CommentEngagement` freezed, `CommentsRepository` real + `FakeCommentsRepository`, remote data source) + `lib/features/post/` (`CommentsCubit` 4-state, `comment_usecases`, `PostDetailPage`, `CommentTile`/`CommentText`/`CommentInput`/`QuickEmojiRow` widgets). **Canonical `commentCount` consistency (SC-005)**: `FeedRepository` gained `watchPost(id)` + `applyCommentCountDelta(id, delta)` (owned by the `AddComment`/`DeleteComment` use cases; `CommentsRepository` never touches the post cache — analyze F1). **No drift schema change, no new pub dependency.** New comment endpoints in `api_endpoints.dart`; `AppRoutes.postDetail`; EN+VI ARB. **349 tests green** (+45 #006: fake-repo, cubit load/add/reply/like/manage, count-consistency, redaction, a11y, adaptive, 4 goldens); `dart analyze` clean.
- **Clarifications locked**: oldest-first ordering · 2,200-char max · count includes replies (delete cascade −(1+replies)) · quick-emoji inserts into input.
- **Follow-ups**: interactive on-device walkthrough → #015; `@mention`/avatar → profile nav when #010 lands; real backend `comments` cutover at dev-backend integration; `bloc_tools:bloc lint` still no local CLI.

### 2026-07-02 — Spec #005 Create Story & Story Tools ✅ MERGED INTO MAIN (PR #6)

- **Shipped (US1–US5 + Polish, 47/47 tasks)**: full-screen nav-less **Create Story** flow (Screen 9) off the contextual "Your story" action — **Pick → Compose → Share**. Single-photo pick (reuses #007 `PhotoLibraryService`); **9:16 compose canvas** with free-form **text + sticker overlays** (drag to position, text ≤100 chars), flattened **WYSIWYG** to JPEG via a `RepaintBoundary` capture on a background isolate (`StoryImageComposer`, preview == export, FR-005/SC-002); **audience** toggle (Your story / Close friends); **resilient upload** (`StoryUploadProgress` + cancel; retry reuses the idempotency key → exactly one story; no partial writes); **24h client-side expiry** (read-time filter via injectable clock); discard-confirm on back-out. Published segment writes into the session-scoped `OwnStoryStore` → bridges the write path to the #004 `FakeStoriesRepository` read path, so "Your story" gets an unseen ring at the front of the rail + plays in the viewer with **zero manual refresh**. Runs DI `environment: 'fake'` (provisional client contract; `env:['real']` stories seam awaits a backend stories spec).
- **Tech notes**: new `lib/features/stories/` compose slice (`StoryComposeCubit` / `StoryGalleryCubit` freezed 4-state, `StoryComposeDraft` / `StoryTextOverlay` models, compose/pick pages + overlay/sticker/text-editor/audience/upload-progress widgets) + `OwnStoryStore` + `StoryImageComposer` in `core/`. **No drift schema change, no new pub dependency** (reuses #007 pipeline as-is; only `StorySegment` gained an optional `audience` field). **304 tests green**; `flutter analyze` clean (2 pre-existing pubspec-sort infos only).
- **Toolchain**: closed on **Flutter 3.44.4 / Dart 3.12.2** (SDK upgraded from a stale 3.41.7 on this machine to match the #003/#007 baseline; this also resolved a `flutter analyze` AOT-snapshot cache crash). The long-deferred **repo-wide golden refresh was executed** here — all goldens (tokens/components/auth/compose + the new `stories/` compose goldens, previously an uncommitted baseline) regenerated under the canonical SDK.
- **Follow-ups**: interactive on-device walkthrough of quickstart scenarios 1–7 → #015 release gate (automated fake-mode coverage is authoritative + green); real backend stories contract → when a backend stories spec lands; `bloc_tools:bloc lint` still not runnable locally (no CLI).

### 2026-07-01 — Spec #007 Create Post (Compose & Upload) ✅ MERGED INTO MAIN (PR #5)

- **Shipped (US1–US5 + Polish, 62/62 tasks)**: full-screen nav-less **Compose flow** (Screens 11–13) off the contextual Create action — **Pick → Edit → Caption → Publish**. Custom in-app gallery grid (`photo_manager`, 4-col, ordered multi-select, cap 10 + message); per-photo **edit** (4:5 crop via `crop_your_image`, preset filters + brightness/contrast/warmth previewed live with `ColorFilter.matrix`, baked on a background isolate by `ImageProcessingService` so preview == export); **multi-photo carousel** (per-item thumbnail strip; swipeable in the #004 `PostCard`, order preserved); **resilient upload** (`UploadProgress` bar + cancel; retry reuses the idempotency key → exactly one post; no partial-cache on failure); **post options** (tag people / location / turn-off-commenting; Stories + music hidden per Q5); **single draft** persisted to drift (schema **v3→v4**), restored across app-kill with keep/discard + drop-missing-assets, wiped on logout. Published post writes the one canonical #004 `Post` → Home feed repaints, no manual refresh. Runs DI `environment: 'fake'`.
- **Tech notes**: new `lib/features/compose/` + reusable pipeline in `lib/core/services/` (`PhotoLibraryService` platform seam +fake +`openSettings`, `MediaUploadService` real/fake, `ImageProcessingService`); `GalleryCubit` + `ComposeCubit` (freezed 4-state). **269 tests pass** (~44 new: bloc + widget + 6 compose goldens + idempotency/rollback + log-redaction + auth-gate + a11y/adaptive). New deps `photo_manager`/`photo_manager_image_provider`/`crop_your_image`/`image` (T001).
- **Follow-ups**: on-device gallery-permission → #015 gate; repo-wide golden refresh needed after the Flutter 3.44.4 upgrade (pre-existing #001/#003/#004 goldens mismatch the new engine); `bloc_tools:bloc lint` not runnable locally (`bloc_lint 0.4.1` ships no CLI).

### 2026-07-01 — Spec #004 Home Feed & Stories ✅ MERGED INTO MAIN (PR #4)

- **Shipped (US1–US5, 64/64 tasks)**: the **first usable surface** — paginated reverse-chronological **Home feed** (`FeedCubit` over `PaginatedListCubit`, `PostCard` wired to real data), **optimistic like/save** with rollback (one canonical cached `Post` in drift, every screen repaints via `watchHomeFeed()`), **StoriesRail** + full-screen **Story viewer** (progress segments, seen-tracking, skip expired/removed segments), pull-to-refresh (clear + repopulate so removed posts drop from cache), empty/offline-from-cache states, adaptive home (bottom-nav `<700` ↔ rail `≥700` + right-rail). drift schema v2→v3 (`StorySeenSegments` + `clearUserScoped`).
- **Tech notes**: **206 tests green** (feed cubit/like/save/no-network/a11y/adaptive/scroll-perf + story viewer/rail/fake-repo). App still runs DI `environment: 'fake'`; `FeedRepository` real seam follows B#004, `StoriesRepository` real seam provisional (backend stories contract TBD — follow-up). Goldens regenerated (sub-pixel toolchain diffs).
- **Follow-ups carried**: on-device long-scroll memory-ceiling profiling → #015 release gate (CI scroll-perf guard T063 is green); deeper moderation/blocked-state handling → #006/#014; `StoriesRepository` real contract → when backend stories land.

---

### 2026-07-01 — Spec #003 Auth & Onboarding ✅ MERGED INTO MAIN (PR #3)

- **Shipped (US1–US5, 75/75 tasks; on-device + dev-backend smoke verified)**: the auth **gate** — Splash (session restore) · first-launch Onboarding (Get started→Sign up / Skip→Sign in) · **Sign in** + **Sign up** (email-only, ≥8 pwd) · **Forgot password** (email **6-digit OTP** + resend cooldown) · **OAuth** Google (always) / Apple (iOS-only, FR-022) · **Profile setup** (live username availability, no avatar). Real `TokenStore`/`TokenRefresher` over `flutter_secure_storage`, `AuthRepository`/`MeRepository` (+ in-memory fakes), `SessionController` (cold-start routing, single-flight refresh reuse, **forced-logout-once + cache wipe**) replacing the #001 `AuthGuardStub`. drift schema v1→v2 (`MeProfiles` + `clearUserScoped`).
- **Tech notes**: **154 tests green**, `dart analyze` clean (2 pre-existing pubspec-sort infos), app still runs DI `environment: 'fake'` (real impls behind `env:['real']`; **`RealTokenStore` env-agnostic** so a fake-issued session persists across restart — analyze finding I1). New deps `google_sign_in ^7.2.0` (v7 singleton API) · `sign_in_with_apple ^8.1.0` · `shared_preferences ^2.5.5`. Added shared `AppTextField` + `OtpInput`. Log-redaction test locks FR-014/SC-008. Goldens regenerated after the **Flutter 3.41→3.44 / Dart 3.12** toolchain bump.
- **Native config done**: **T002** iOS (deployment target 13 + Sign in with Apple capability/entitlement in `Runner.entitlements` + Google reversed-client-id URL scheme in `Info.plist`) · **T003** Android (`minSdk 24` + `launchMode="singleTop"`). **T072** quickstart scenarios 1–8 (fake mode) + **T074** on-device/dev-backend smoke — all marked complete (75/75). OAuth client-id/Service-ID provisioning is per-environment credential config, supplied at real-backend cutover (#004+ dogfood).

---

### 2026-06-30 — Spec #002 Networking, Cache & Realtime Core ✅ MERGED INTO MAIN

- **Shipped**: `ApiClient` (single `Dio`) behind idempotency → auth-token → **single-flight refresh** → redacted-logging interceptors; centralized HTTP→`AppFailure` mapping (`FailureMapper`, envelope `{error:{code,message,details}}`, contract-stable `code`s); cursor pagination (`CursorPage<T>`) + reusable 4-state `PaginatedListCubit<T>`; **drift** cache base (`AppDatabase` + `UsersDao`, reactive `.watch()` reads) + migration harness; `RealtimeClient` **Socket.IO** scaffold + typed events + fake (wired in #012/#013); repository pattern + in-memory fakes proven by the `User` reference slice. App runs DI `environment: 'fake'` — fully offline/zero-network. **No auth UI/session persistence** (that's #003).
- **Tech notes**: 50/50 tasks; **103 tests green**, `dart analyze` clean. Token seams `TokenStore`/`TokenRefresher`/`AuthEventsSink` are fakes now — real impls land in #003. Endpoints/events centralized in `core/constants/{api_endpoints,socket_events}.dart` (no inline literals).
- **Decisions resolved**: local cache engine = **drift** (over hive); realtime transport = **`socket_io_client`** (backend gateway is Socket.IO) → constitution PATCHed to v1.0.2. `drift_dev` pinned (2.34.1 needs analyzer 13).
- **Follow-ups carried**: `RealtimeClient` wiring deferred to #012/#013; `TokenStore`/refresh real impls + `flutter_secure_storage` land in #003; on-device/dev-backend socket + refresh smoke test deferred to feature specs.

---

### 2026-06-30 — Spec #001 Project Foundation, Design System & Navigation ✅ MERGED INTO MAIN

- **Shipped**: Clean Architecture shell (`core/` + `features/`), dev/prod flavors + entry points, auth-guarded `go_router` skeleton (pre-auth flow vs 5-tab `StatefulShellRoute`) with placeholder destinations on mock data, **adaptive shell** (phone bottom-nav `<700` ↔ tablet `SidebarRail` `≥700`, compact/full by width) + reusable two-pane/master-detail primitive, **fixed light/dark token system** (`AppColorsX` ThemeExtension + `AppColors`/`AppTypography`/`AppGradients`/spacing/radius/shadow/motion), full **shared component library** (`AppButton`/`AppIconButton`/`AppIcon`/`Avatar`/`PostCard`/`BottomNav`/`SidebarRail`/`StoriesRail`/`TopBar`/`Toast`/`ActionSheet`/`AppDialog`…), foundation primitives (`Result<T>`/`AppFailure`/`AppCubit` 4-state/`AppLogger`/formatters), DI, EN+VI ARB l10n. **No networking/auth/persistence** (those are #002/#003).
- **Tech notes**: 73 Dart files; 46 tests green incl. **12 goldens** (token gallery + PostCard/Avatar/BottomNav/SidebarRail, light+dark) — real brand fonts loaded in tests via `test/flutter_test_config.dart`; a hardcoded-color/`TextStyle` CI guard enforces the token discipline (T078a).
- **Follow-ups carried**: bounded `cacheWidth`/`cached_network_image` for feed thumbnails deferred to #004 (no image decode active at #001 — mock providers are null → placeholder surfaces); true on-device VoiceOver/TalkBack + physical-rotation recording deferred to the #015 release gate (automated a11y/text-scale/adaptive coverage is green).
- **Decisions still open for #002**: local cache engine (drift vs hive); dev/prod API base URL + realtime endpoint; bundle ids.

---

_Project bootstrap (constitution v1.0.0 + `.claude/claude-app/` docs) is recorded in [`project-context.md`](project-context.md), not here — this file tracks shipped feature specs only._
