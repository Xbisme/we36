# Alignment Decisions

> One file per spec that had a pre-`/speckit.specify` alignment session, archived here after the spec merges (see [`../dev-workflow.md`](../dev-workflow.md) §Per-Spec Hygiene step 6).
>
> Naming: `spec-<NNN>-<topic>.md` (e.g. `spec-002-cache-engine.md`).

_No decisions archived yet._
